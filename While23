#include <iostream>
#include <math.h>
#include <conio.h>
using namespace std;
int main() {
 int A, B;
    cout << "Imput A";
    cin >> A;
    cout << "Imput B";
    cin >> B;
    while ((A != 0) & (B != 0)) {
        if (A > B) A = A % B;
        else B = B % A;
    }
    cout<<"NOD"<< A + B;
}
